# EXP-0005：固定状态上的价值与表征训练阶段图谱

状态：`preregistered`，高成本运行：`not approved`，人工看图：`pending`。

## 一句话问题

在同一组500个冻结状态上，Nature DQN从0.25M到10M决策的Q分布、动作置信度与FC512整体几何如何变化，这些变化与既有完整游戏评估阶段是否只呈描述性共变？

## 阶段与基座

- 阶段：`exploration`
- 当前主要矛盾：在不追加长训练的前提下，构建可追溯离线数据和统计协议，区分网络随训练的价值、表征、视觉敏感性与行为变化，并明确相关性、校准与因果边界。
- Canonical baseline：`DQN2015-NATURE-BREAKOUT-INDEPENDENT`
- 模板：`probe`；证据权限：`diagnostic_only`
- 假说族：`dqn-fixed-state-stage-atlas`；工作模式：`mixed`
- 唯一变量：观测性训练阶段：40个冻结checkpoint的completed_agent_decisions；状态ID、网络结构、动作语义和提取器保持不变。
- 预期动作：40个checkpoint均成功加载，对同一500状态产生有限的Q[500,4]与FC512[500,512]，并与训练期held-out mean max-Q逐阶段一致。
- 完成正向信号：40 x 500 Q与FC512数组、输入/checkpoint SHA256 manifest和机器表存在。；完整forward重构误差<=1e-6且逐阶段held-out mean max-Q误差<=1e-5。；生成一张价值、表征与行为同坐标审查图，并分开记录观察与解释。

## 假说

若EXP-0004的行为提升伴随稳定的内部组织而非单纯Q尺度膨胀，则回报进入高分阶段时action margin与表征几何指标应出现可复算的阶段变化，同时mean max-Q保持有限且所有变化在相同500状态上成立。


## 替代解释

- 500个状态来自训练早期replay，其状态覆盖偏差可能使固定面板变化不代表后期策略访问分布。
- effective rank或CKA变化可能来自激活尺度与ReLU稀疏性，而非更好的任务表征。
- Q、表征与score的同步变化可能仅由共同训练进度或评估噪声驱动，不构成相互因果。

## 控制项

- 每阶段使用完全相同且按state_id配对的500个uint8 NCHW状态。
- 固定QNetwork结构、动作顺序、归一化、batch逻辑与final linear重构检查。
- 完整游戏raw return只作行为描述，不与训练Q作校准差。

## 主指标

- 逐阶段max-Q分位数、action margin分位数、greedy action比例与原held-out-Q parity。
- FC512 effective rank、participation ratio、PCA top10/top50 fraction、ReLU zero fraction及到10M的linear CKA。
- 40阶段行为mean/median return与上述量的描述性时序对齐；不作因果或跨seed推断。

## 证据四联

- 数值/概率：
- 空间/视觉：
- 时序/上尾：
- 因果干预：

## 停止条件

- 任一输入或checkpoint缺失、step错位、hash/provenance不完整时停止。
- 任一Q/feature非有限、forward误差>1e-6或held-out parity误差>1e-5时停止。
- 输出预计超过2GiB或发现分析代码与冻结commit漂移时停止。

## 人工审查

`/root/autodl-tmp/artifacts/dqn2013/review/EXP-0005-fixed-state-value-representation-atlas`

## 最终裁决

- Verdict：`fixed_state_atlas_completed_q_scale_not_behavior_proxy`
- Decision：`promising_unresolved`
- 解释：在该固定seed与固定状态面板上，Q绝对尺度不是策略质量的充分代理；action margin是解释9.25M局部退化的更有区分度候选，但尚需匹配训练语义的轨迹校准和输入干预。
- 预期动作：`occurred`；完成信号：`confirmed`
- 进展：`uncertainty_reduction`，用40 checkpoint固定面板排除了Q绝对尺度这一简单行为代理，并把后续计算集中到9.25M局部异常。
- 耗时：wall `0.200h`；compute `0.010h`
- 观察：40 x 500固定状态Q/FC512面板通过全部parity与有限性检查，并与40次既有行为评估同阶段对齐。；9.25M出现全程最高mean max-Q但低回报、低margin和CKA回撤；10M在更低Q尺度下达到350.18回报和最高margin。；原始阶段高相关在一阶差分后显著减弱，表征rank也非单调，因此共同训练进度与局部波动必须分开。
- 限制：仅一个训练seed，不能估计训练稳定性或跨seed机制一致性。；500个状态来自早期replay，不能代表每个checkpoint的on-policy访问分布。；完整游戏raw-return评估与life-loss/clipped reward训练Q语义不同，本周期未计算Q-return calibration gap。；阶段相关和blur前的固定输入推理都是诊断证据，不支持Target Network、表征质量或对象语义的因果主张。
- 下一步：9.0M/9.25M/9.5M/10M在匹配clipped reward、life-loss terminal、gamma=0.99的greedy轨迹上，9.25M是否仍表现为系统性Q-return gap或低action margin？
- 人工确认：`pending`
- Scoreboard：`not_required`；Claim registry：`not_required`
